"""PII detection and filtering engine for task mining events.

Implements Layer 2 (at-source, on-device) and Layer 3 (server-side) PII
detection using regex patterns and configurable redaction strategies.
"""
