/**
 * @deprecated Import from "@/lib/api" resolves to this file for backwards
 * compatibility.  The implementation has been split into domain-specific
 * modules under `./api/`.  This file simply re-exports everything from the
 * barrel so that all existing importers continue to work without changes.
 */
export * from "./api/index";
