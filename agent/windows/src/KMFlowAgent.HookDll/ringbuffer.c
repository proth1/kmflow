/**
 * Ring buffer implementation.
 *
 * All logic is in the header (inline functions) for performance.
 * This .c file exists only as a compilation unit to satisfy the
 * build system — no additional code needed.
 */

#include "ringbuffer.h"
